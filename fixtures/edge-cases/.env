# Kafka is configured by env var only — there is NO kafka client library in
# requirements.txt, so env detection is the only signal for this store.
KAFKA_BROKERS=broker1:9092,broker2:9092

APP_ENV=production
LOG_LEVEL=info
